#ifndef TORSO_H
#define TORSO_H

#include <cmath>
#include <vector>
#include <iostream>

#include "Triangle.h"

class Torso : public Triangle {

public:
	Torso(){
		//parseVertexInfo("Torso");

		//Matrix4 tangram = Matrix4::IDENTITY;
		//tangram = tangram.rotateZ(180.0f);
		//tangram = tangram.translate(0.0f, 0.98f, 0.02f);

		//Matrix4 square = Matrix4::IDENTITY;
		//square = square.rotateX(-90);
		//square = square.rotateY(-135);
		//square = square.translate(0.0f, 0.4f, 0.0f);

		//_transformationMatrix.push_back(tangram);
		//_transformationMatrix.push_back(square);
	}

};
#endif
