#ifndef TAIL_H
#define TAIL_H

#include <vector>
#include <iostream>

#include "Triangle.h"

class Tail : public Triangle {

public:
	Tail(){
		//parseVertexInfo("Tail");

		//Matrix4 tangram = Matrix4::IDENTITY;
		//tangram = tangram.rotateZ(90.0f);
		//tangram = tangram.translate(0.8f, 1.0f, -0.1f);

		//Matrix4 square = Matrix4::IDENTITY;
		//square = square.rotateX(-90.0f);
		//square = square.rotateY(-90.0f);
		//square = square.translate(-0.58f, 0.33f, -0.58f);

		//_transformationMatrix.push_back(tangram);
		//_transformationMatrix.push_back(square);
	}

};
#endif
